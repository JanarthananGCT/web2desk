{
  "name": "app name",
  "version": "1.0.0",
  "description": "projectDescription",
  "homepage": "homepage link",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "dist": "electron-builder",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "build": {
    "appId": "Ideopedia1.0.0.0",
    "dmg": {
      "title": "${productName} ${version}"
    },
    "linux": {
      "target": [
        "AppImage",
        "deb"
      ]
    },
    "win": {
      "target": "nsis"
    }
  },
  "author": "Admin <admin-email>",
  "license": "ISC",
  "devDependencies": {
    "electron": "^20.0.2",
    "electron-builder": "^23.6.0"
  }
}
